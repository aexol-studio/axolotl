type Beer implements Node{
	name: String!
	price: Int!
	_id: String!
	createdAt: String!
	info:String
	url: URL
}

type Query{
	beers: [Beer!]
	testAuth: String! @auth
}

type Mutation{
	addBeer(
		beer: CreateBeer!
	): String
	deleteBeer(
		_id: String!
	): Boolean
	updateBeer(
		beer: UpdateBeer!
		_id: String!
	): Boolean
}

input CreateBeer{
	name: String!
	price: Int!
	url: URL
}

interface Node{
	_id: String!
	createdAt: String!
}

input UpdateBeer{
	name: String
	price: Int
	url: URL
}

schema{
	query: Query
	mutation: Mutation
}


scalar URL
directive @auth on FIELD_DEFINITION